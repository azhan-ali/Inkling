/**
 * Gemini client (Phase 1 stub).
 *
 * The real implementation will live here, using `@google/generative-ai`
 * with `responseMimeType: 'application/json'` and a strict response schema.
 */

export async function generateWithGemini(): Promise<never> {
  throw new Error("generateWithGemini is not implemented yet — built in Phase 1.");
}
