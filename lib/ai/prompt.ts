/**
 * Master system prompt for Inkling.
 *
 * Phase 1 will replace this stub with the full prompt engineering layer,
 * including Solana ecosystem context injection, archetype scoring logic,
 * and tone calibration rules.
 */

import type { InklingInputs } from "@/types/inkling";

export function buildPrompt(_inputs: InklingInputs): string {
  return "// Phase 1 will build this prompt.";
}
