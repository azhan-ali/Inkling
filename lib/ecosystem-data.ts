/**
 * Solana ecosystem intelligence — hardcoded so outputs feel native.
 *
 * Phase 1 will populate this with:
 *   - KEY_HANDLES: key Solana accounts with context tags
 *   - LIVE_DEBATES: current debate topics per category
 *   - TIMING_WINDOWS: optimal posting hours (UTC)
 *   - CONTENT_FORMATS: what works for the Solana builder audience
 *   - ARCHETYPES: 4 definitions with emotion + spread mechanics
 */

export const ECOSYSTEM_DATA_STUB = "populated in Phase 1" as const;
